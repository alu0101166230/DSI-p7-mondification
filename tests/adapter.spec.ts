/*import 'mocha';
import {expect} from 'chai';
import {Adapter} from '../src/adapter';
import {ImperialLenght} from '../src/imperial_lenght';

describe('Adapter', () =>{
  const adapter = new Adapter(100);
  const imperial = new ImperialLenght();

  it('Metric must retunr unit by cm', ()=> {
    adapter.setFeet(100);
    expect(adapter.getLenght()).to.be.equal(100);
  });
});



describe ('Adapter', () =>{
    it('Metric must retunr unit by cm', ()=> {
        expect(2+2).to.be.eq(4);
    });
})

*/

